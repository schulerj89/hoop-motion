# KineRig Studio v1.2.0 Artifacts

Minor release artifacts generated by the smoke flow.

- `posebot-auto-rig.json` - exported neutral model rig profile after Auto A/T fill.

The stock-video acquisition workflow writes local candidate/download metadata under ignored `data/search/` and `data/input/*.source.json`. Processed run provenance is stored in `public/runs/<run>/source.json` and indexed by `public/runs/index.json`.
