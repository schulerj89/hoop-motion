# KineRig Studio v1.4.0 Artifacts

Minor release artifacts generated by the smoke flow.

- `posebot-auto-rig.json` - exported neutral model rig profile after Auto A/T fill.

The `Acquire` UI uses local middleware and ignored local key/search/download files. No provider credentials are committed.
